# Decky PluginLoader is x86-64; on aarch64 it runs via FEX/Box64 binfmt.
# The unit runs as root (upstream Decky) but FEX RootFS lives under the user.
[Service]
Environment=HOME=/home/steamos
Environment=XDG_CONFIG_HOME=/home/steamos/.config
Environment=XDG_DATA_HOME=/home/steamos/.local/share
