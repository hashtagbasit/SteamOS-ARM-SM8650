# Start speaker mixers when the ASoC card appears (system unit never
# logged from multi-user.wants on ostree overlay boots).
SUBSYSTEM=="sound", KERNEL=="controlC0", TAG+="systemd", ENV{SYSTEMD_WANTS}+="sm8550-audio-setup.service"
