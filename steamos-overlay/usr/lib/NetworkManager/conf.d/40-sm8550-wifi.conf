[device]
# This kernel has no CONFIG_CRYPTO_USER_API_HASH/SKCIPHER, so iwd exits
# ("No HMAC(SHA1) support"). Same kernel on the custom image uses
# NetworkManager + wpa_supplicant (openssl), which works.
wifi.backend=wpa_supplicant
