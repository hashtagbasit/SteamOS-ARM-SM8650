# Decky PluginLoader spawns children (e.g. Decky LSFG-VK). With KillMode=process
# those orphans survive stop and block systemd-shutdown for ~minutes.
[Service]
KillMode=control-group
TimeoutStopSec=5
FinalKillSignal=SIGKILL
SendSIGKILL=yes
