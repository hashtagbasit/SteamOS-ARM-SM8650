# Steam ROM Manager (vendor)

Prebuilt arm64 Electron app — **not stored in git** (GitHub 100 MB limit).

At build time:

```bash
./scripts/fetch-steam-rom-manager.sh
```

Pinned version: **2.5.44** (`SRM_VERSION` env override).

Source: [SteamGridDB/steam-rom-manager releases](https://github.com/SteamGridDB/steam-rom-manager/releases)
