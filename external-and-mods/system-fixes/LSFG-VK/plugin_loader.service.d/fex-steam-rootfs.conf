# Decky PluginLoader is x86-64; on aarch64 it runs via FEX binfmt.
# The unit runs as root (upstream Decky) but FEX RootFS lives under steam's XDG paths.
[Service]
Environment=HOME=/home/steam
Environment=XDG_CONFIG_HOME=/home/steam/.config
Environment=XDG_DATA_HOME=/home/steam/.local/share
